# Common ignores
README.md
LICENSE
SOFTWARE.md
install.sh
scripts
Zed

# GUI-only files
{{ if not .isGui }}
.atom
{{ end }}

# Windows-only files
{{ if ne .chezmoi.os "windows" }}
AppData
.wslconfig

# Windows and GUI-only files
{{ if not .isGui }}
AppData/Roaming/Zed
{{ end }}

{{ end }}

# Ignore on Windows
{{ if and (ne .chezmoi.os "linux") (ne .chezmoi.os "android") }}
.config/nushell
{{ end }}

# macOS-only files
{{ if ne .chezmoi.os "darwin" }}
Library
{{ end }}

# Unix-only files
{{ if and (ne .chezmoi.os "linux") (and (ne .chezmoi.os "darwin") (ne .chezmoi.os "android")) }}
.config/helix

# Unix and GUI-only files
{{ if not .isGui }}
.config/zed
{{ end }}

{{ end }}
